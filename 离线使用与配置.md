# 离线便携包与配置

## 当前电脑直接使用

已安装位置：`D:\07_工具与资产\02_工具与软件\项目知识工作台`。

1. 打开上述目录，双击 `启动工作台.cmd`。不要在ZIP内部运行。
2. 在本机浏览器访问 `http://127.0.0.1:8765/`。服务已运行时直接打开此地址即可。
3. 在“配置 → 数据”核对资料根目录为 `D:/`；项目空间读取 `D:/05_项目与交付`，知识库读取 `D:/03_技术知识库`。
4. 启动窗口保持打开。退出时先保存草稿，再在启动窗口按 Ctrl+C；仅关闭网页不会停止服务。

不要同时启动指向同一资料目录的多个工作台实例。

## 新电脑首次部署

1. 准备 Windows 10/11 x64 和新版 Edge 或 Chrome。
2. 将 `项目知识工作台-v2.5.0-Windows-x64.zip` 完整解压到可写目录，例如 `D:\Tools\项目知识工作台`。
3. 使用全新资料库时保留默认 `config.json`；接入已有资料时按下方示例设置 `root`。配置根目录不会自动搬运旧资料。
4. 双击 `启动工作台.cmd`，检查项目空间、知识库列表和图谱能打开。
5. 按需配置模型。不使用模型也能离线阅读、手动编写草稿并审核入库。

包内关键内容：

| 文件或目录 | 用途 |
|---|---|
| 启动工作台.cmd | 双击启动入口 |
| runtime/ | 内置Python运行时，无需额外安装 |
| vendor/ | 离线Office阅读组件及许可证 |
| config.json | 资料根目录与监听端口 |
| data/ | 运行后生成的草稿等工作数据，升级需保留 |
| workspace/ | 默认配置下生成的项目与知识资料目录 |
| 使用说明.md | 新增项目、资料选择、草稿审核与知识沉淀流程 |
| 项目记录模板.md | 可复用的需求、版本、运行及问题记录模板 |
| SHA256SUMS.txt | 分发文件完整性校验清单 |

适用系统：Windows 10/11 x64。完整解压ZIP到可写目录，双击“启动工作台.cmd”，默认浏览器打开本机网页。已内置Python与PDF文字提取库，不需要安装Python、Node或联网下载依赖。启动窗口保持打开；关闭窗口即停止服务。网页依赖本地服务，不能直接双击index.html代替启动。

离线可用：项目浏览与创建、文档提取与预览、人工草稿编辑与保存、审核入库、知识检索与阅读。所有界面资源随包提供，无CDN。资料保存在普通目录，知识以Markdown保存，可用其他编辑器打开。

模型功能：DeepSeek、通义等云端接口需要联网；离线提炼可用已部署且已下载模型的本机OpenAI兼容服务，例如基础地址 http://127.0.0.1:1234/v1 。填写该服务实际模型名称；无需认证时密钥留空。本包不包含模型权重或模型推理服务。没有模型时，使用人工草稿流程。

## 资料位置

分发包默认配置如下，首次运行在包旁创建独立workspace，不包含你的项目文件或密钥：

```json
{
  "root": "./workspace",
  "port": 8765
}
```

要访问已经整理好的D盘，关闭服务后修改config.json：

```json
{
  "root": "D:/",
  "port": 8765
}
```

root是包含“05_项目与交付”和“03_技术知识库”的父目录。相对路径以工具所在目录为基准。端口冲突时可改成8766，重新启动。仅监听127.0.0.1，可由本机Edge、Chrome或内置浏览器访问，不提供局域网多人访问。

## 保存与升级

正式知识在root下的“03_技术知识库”；项目原件在“05_项目与交付”；本地草稿在工具data目录。备份时同时保存workspace（或配置的实际资料目录）、data及config.json。升级时将新版解压到另一位置，再复制自己的config.json和data；如果使用相对workspace，也要完整转移该目录，不能用空目录覆盖原资料。

升级操作顺序：保存草稿 → 停止旧服务 → 备份资料和配置 → 将新包解压到新目录 → 复制原配置与工作数据 → 启动新版本 → 检查项目、草稿、知识检索和文件预览。验收前保留旧安装包和备份。回退前停止新服务并备份新增资料，再恢复升级前的应用与配套数据；不要覆盖升级后新增或修改的文件。

供应商地址、API Key、模型列表、默认模型、effort与界面偏好保存在浏览器本地存储，**不在config.json、项目、知识库或data目录内**。换电脑、换浏览器、清除浏览器数据或更换端口后，需要重新填写这些设置。桌面版保存在应用数据的browser目录，升级继续使用同一应用数据时可保留。

## 模型配置与离线运行

在“配置 → 模型”新增兼容OpenAI的供应商，填写基础地址、实际模型名称，设置默认模型并测试连接。地址不包含 `/chat/completions`。远程服务地址需要HTTPS。

Key在配置页随供应商持久化到当前设备的浏览器数据中，不通过工作台同步上云。发起模型调用时，它会作为鉴权信息发送给所选模型服务商。删除供应商将同时删除对应本机Key；共享Windows账户不建议保存生产Key。

完全离线时，先独立部署本机OpenAI兼容推理服务并下载模型权重，再将其基础地址（例如 `http://127.0.0.1:1234/v1`）和实际模型名称填入配置。本工作台不代为安装推理服务或下载模型。云模型只有在联网时可用；选择资料后，先核对待发送文字，再点击生成。

## 部署验收与常见问题

| 检查或现象 | 操作 |
|---|---|
| 页面打不开 | 确认启动窗口仍运行；使用127.0.0.1与配置端口，不直接打开index.html |
| 端口被占用 | 停止你已启动的旧工作台，或在config.json更换port后启动；不要结束不明进程 |
| 使用localhost提示拒绝访问 | 工作台网页使用 `http://127.0.0.1:8765/` |
| 项目或知识库为空 | 核对root是两类目录的共同父目录；新包默认workspace为空，不包含本机D盘资料 |
| 保存目录设置后未变化 | 保存草稿并重启服务；如设置过WORKBENCH_ROOT/WORKBENCH_PORT环境变量，它们优先于配置文件 |
| 图谱目录如何展开 | 双击目录节点直接展开；单击查看信息；键盘选中后按Enter展开 |
| Office预览不完整 | 参考下方格式和大小限制；复杂版式下载原件，用本机Office查看 |
| 模型测试失败 | 检查地址、模型名称、Key和服务是否运行；云端接口还需网络与可用额度 |
| 换浏览器后模型配置丢失 | 在该浏览器重新配置；项目与知识文件不受影响 |

验收时先确认资料根目录，再打开一份已知文档、搜索一个已知知识文件、双击一个图谱目录。全新工作区可新增测试项目并走一次“手动沉淀 → 保存草稿 → 审核入库”；现有正式资料库无需为部署验证新增测试文件。

当前服务仅绑定本机127.0.0.1，不支持手机、其他电脑或局域网多人访问；不能把开放端口当作多人部署。离线网页打开仍需本机服务运行，无需互联网。

## 设计参考

- 飞书知识库：以工作入口聚合项目与知识，减少查找步骤。https://www.feishu.cn/product/wiki
- Wiki.js：主题导航、Markdown阅读与内置搜索。https://js.wiki/
- Obsidian：本地文件作为知识载体，可继续由其他工具维护。https://help.obsidian.md/Files+and+folders/Manage+notes

此版本是本地单人工作台，没有复制这些产品的代码，不提供多人权限、实时协同、完整双向链接图谱或其全部功能。


## 项目资料原件预览（2.3）
在项目资料列表点击“预览”，可在当前项目中阅读原件，选择“展开阅读”放大。无需安装 Office，全部组件随离线包提供，原件不上传第三方。Word（DOCX）支持正文、表格、常见图片及分页；复杂域、EMF/WMF图片等以本机 Office 为准。Excel（XLSX/XLS）支持切换工作表，每表显示已用区域前200行、40列，公式仅显示已保存的计算结果，不呈现图表，不执行宏；Excel目前仅供阅读，不能勾选为模型证据。PDF可逐页阅读、翻页和缩放；加密或特殊字体文件可能需要本机阅读器。MD/TXT可查看文字。旧DOC及PPT/PPTX尚未支持。单文件12MiB，Office压缩包展开大小上限64MiB。
“预览待发送资料”仍展示提炼实际使用的文字内容，只有点击发送生成才调用配置的模型接口。
